apiVersion: v1
kind: Service
metadata:
  name: {{ include "chainthru-server.fullname" . }}
  labels:
    {{- include "chainthru-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chainthru-server.selectorLabels" . | nindent 4 }}
